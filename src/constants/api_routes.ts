export const API_BASE_URL = 'http://127.0.0.1:3000';

export const REGISTER_USER = `${API_BASE_URL}/register`;

export const LOGIN_USER = `${API_BASE_URL}/login`;

export const CREATE_CHATROOM = `${API_BASE_URL}/chat_rooms`;

export const CREATE_MESSAGE = `${API_BASE_URL}/messages`;